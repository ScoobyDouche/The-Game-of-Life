# The-Game-of-Life
Automated installer for The Game of Life (Hasbro Interactive, 1998) on Linux Mint/Ubuntu via Wine. The disc already ships the game pre-extracted, so the script just copies it into a dedicated 32-bit Wine prefix, writes the registry keys, and builds a fake CD-ROM drive so the disc isn't needed at runtime. One command from disc to playable.
